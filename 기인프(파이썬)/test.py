arr[8][3]
for i in range(8):
    temp=input().split()
    score=list(map(int,temp[0].split(":")))
    team=if(temp='B')0
    last_score=score[0]*60+score[1]+score[0]*0.001
    arr[i][0]=last_score
    arr[i][1]=team
    
    